# frozen_string_literal: true

RSpec.describe Restforce::Orm do
  it "has a version number" do
    expect(Restforce::Orm::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
